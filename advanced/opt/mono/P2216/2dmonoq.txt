用方块左上角的坐标代表方块坐标
求出每个长条的最大值：
	对于每一列j
	遍历i(1~a-n+1)(总指向待添加的数)
	检查队首是否过期(<=i-n)，出队直到不过期
	队尾出队直到待添加的数小于队尾
	i-n(如果>0)的长条最大值更新为队首对应的值
更新每个方块的最大值：
	对于每一行i，
	从左向右遍历列j(1~b), 对每个列数为j，行数为i~i+n-1的长条，获取最大值。
	从队首清空过期(列数<j)的列数，从队尾清空队列中小于等于目标的列数，j入队
	将最大值更新为队首的最值
